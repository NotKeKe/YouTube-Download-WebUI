version: '3.9'

services:
  webapp:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-8127}:8127"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
