version: '3.9'

services:
  webapp:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-8127}:8127"
    environment:
      RUNNING_IN_DOCKER: "1"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
